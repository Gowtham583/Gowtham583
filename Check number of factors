#include <stdio.h>

int main() {
    int num, count = 0;

    printf("Enter a positive integer: ");
    scanf("%d", &num);

    for(int i = 1; i <= num; i++) {
        if(num % i == 0) {
            count++;
        }
    }

    printf("Number of factors of %d: %d\n", num, count);
    return 0;
}
